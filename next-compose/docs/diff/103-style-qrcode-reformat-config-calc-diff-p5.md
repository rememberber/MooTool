# DIFF-103：二维码 / 格式化 / 配置 / 计算器 / 对比 P5 按钮

- 编号：DIFF-103
- 影响：F02 文本对比；F03 格式化；F06 配置转换；F17 二维码；F21 计算器
- 日期：2026-09-15

## 原行为（Electron）

- 各 P5 工具工作区内操作条、中栏转换钮、模式切换为 `panel-command`（约 30px）

## 本产品行为

- **二维码**：纠错下拉、Logo 清空、生成/保存/复制；识别剪贴板/识别/结果复制 → `p5Toolbar`
- **格式化**：类型/缩进下拉、格式化与复制/保存/清空（含溢出）→ `p5Toolbar`
- **配置**：导入导出、双向转换、校验/格式化中栏 → `p5Toolbar`
- **计算器**：等号、进制转换、排列组合等操作钮 → `p5Toolbar`
- **文本对比**：对比/导航/导入与第二行高亮/布局切换（`primary` 保留）→ `p5Toolbar`

## 证据

`desktopTest` 见 `docs/acceptance.md`。

## 未做

各工具标题栏历史/分离仍为默认密度；file-drop 拖放、产品窗 Tab/IME、三平台安装仍未测。
