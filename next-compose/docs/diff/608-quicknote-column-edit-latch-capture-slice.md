# DIFF-608：F01 随手记列编辑闩锁 Tab 焦点帧 203

基线：DIFF-607（工作区）。

## 范围

- **F01/P0**：`QuickNoteColumnEditLatchCaptureTest` → `203-compose-quicknote-column-edit-latch-tab-focus.png`（与 F04 帧 `201` 对称，**不重复** 产品窗 `125`）。
- **证据 hint**：`mootool_evidence_print_editor_ime_column_latch_tab_focus_hint` 登记 F01 帧。

**不重复** 607：Vault §A / 帧 202。

## 验证

JDK 21：`./gradlew :composeApp:desktopTest --offline`
