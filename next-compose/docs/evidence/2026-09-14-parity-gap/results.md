# Electron 对照缺口（2026-09-15）

- 已执行：对照 Electron `next/` 与 `docs/feature-parity.md`/`docs/ui-spec.md`
- 最近切片：DIFF-516 命令盘设置分类深链（`CommandSearchCatalog`，`mcp`/git/proxy 等→`openSettings`）、F04 `listPaths` Electron fixture、`HostEngine.matchesProfileSearch`、Vault Git push 冲突期 UI 禁用；DIFF-515 Vault Git merge/rebase 进行中状态胶囊 + `git.counts`（冲突上下文）+ `GitEngine.push` merge/rebase 守卫、F09/F20 `NetworkSettings.toHttpProxyConfig()`、`docs/fixtures/electron-next-systemService-vitest.md`；DIFF-514 设置网络代理/超时 `SettingCommitTextField` + `SettingsNetworkNormalize`、Vault Git 面板 `commitGitRemote` 前置校验；DIFF-513 产品窗冲突/Git merge/IME 准备脚本共享校验（`product-evidence-common.sh`、`verify-product-evidence-prep.sh`、`ProductEvidencePrepScriptTest`）；DIFF-512 F23 `ScreenRegionPicker` 框选后 move/resize/Enter 确认（`ScreenCaptureInteraction` + overlay UI）、命令盘 Tab 走查链单测；DIFF-511 macOS `MOOTOOL_P7_BUILD_DIST=1` 本机 DMG 烟雾（`results.md`/`run.log`）、F23 `ImageEngine` 区域截图 `move`/`resize` 几何对齐 Electron `captureSelection`；DIFF-510 设置 `SettingsGroup` `--surface` 行块、命令盘搜索行帧 `151`、modern 非 p5 压平壳帧 `150`；DIFF-509 设置 Vault Git 失焦提交 + 非法 remote toast、`commitGitRemote`、`MOOTOOL_P7_BUILD_DIST` 可选本机 `packageDistributionForCurrentOS`、Vault/Git 设置走查 `results.md`；DIFF-508 非 p5 `mooToolShell` 默认压平、ja 设置分类标题、`EnvEngine` Electron 环境文件 fixture、Vault Git remote 加载 sanitize + 设置走查脚本、`verifyNativePackageMetadata`；DIFF-507 设置分类导航/顶栏 chrome（`settings.category.*` + 图标）、命令盘 `Locale.ROOT` 搜索、Vault Git 加载修剪、`NetEngine` 端口 fixture、hero/claude `flattenWorkspaceToolPanels` 单测、P7 `prepare-p7-package-smoke.sh`；DIFF-506 代码运行 `runtime.options` 长度规范化（arguments 2000 / workingDirectory 1000 / 草稿 1 MiB，对齐 Electron `normalizeRuntimeOptions`）；DIFF-505 `MooGhostButton` 圆角（对齐 Electron `.icon-ghost` modern 9dp/默认 8dp，quiet 仍为 8dp 非 `.icon-button` 6dp）；DIFF-504 `MooIconButton` 圆角/34dp 方钮/modern 浅阴影（对齐 Electron `.icon-button` 的 `--desktop-control-radius`，非 toolbar 7dp）；DIFF-503 modern 非 p5 `MooButton` 7dp 圆角（对齐 Electron `:root[data-interface-style='modern'] .toolbar-button`）；DIFF-502 modern/quiet 非 p5 `MooButton` 排版（13sp/500、primary 浅阴影，对齐 Electron `.editor-toolbar .toolbar-button`）；DIFF-501 modern/quiet 工具页 `p5Toolbar` 内容区密度（高度跟 `controlHeight`、13sp/500、primary 浅阴影，对齐 Electron `.tool-page .toolbar-button`）；DIFF-500 设置 `general.language`/`closeBehavior` 加载/Electron 迁入规范化（对齐 Electron `normalizeSettings` general 切片）；DIFF-499 `EditorFindShortcutPolicy` 壳层查找快捷键与 `EditorFindToolScope` 接线（HTTP 仅 F）；DIFF-498 分离工具窗 `AppMooTheme`（与主窗同传 `compactNavigation`/外观）、`EditorFindToolScope` 对照 Electron JSON/随手记/Host/HTTP 查找与 F05 扩展、紧凑 p5 工具栏 Compose 帧；DIFF-497 classic 导航分组标题/顶距、紧凑导航下 p5 工具栏 dense、设置快捷键切换与左侧 ↑↓ 分类、F05 源码 RSTA 查找；DIFF-496 工作台紧凑导航（侧栏项 30dp/12sp、`showSeparators` 分组底分隔线，对齐 Electron `app-shell--compact-nav` / `app-shell--nav-separators`）；DIFF-495 设置 `appearance.fontFamily` 加载/Electron 迁入规范化（`system-ui`→`system`、空值回退，补 DIFF-490 界面字体链）；DIFF-494 rebase 冲突期 pull/commit/continue/abort 守卫（`GitRebaseGuardTest`，对齐 Electron `VaultGitService`）；DIFF-493 Vault 外部删除冲突另存副本（JSON/随手记 `deleted` 分支）+ Git `continueOperation` 未解决冲突拒绝；DIFF-492 命令盘 Tab 序搜索→关闭→选中结果（对齐 Electron `CommandPalette` 搜索行 `input`→关闭钮→结果按钮）；DIFF-491 命令盘 Tab/Shift+Tab 在搜索框与选中结果行间切换焦点（对齐 Electron 结果按钮可 Tab 聚焦）；DIFF-490 设置 `accentColor` 加载/Electron 迁入规范化（对齐 `accentColorPresets`，`orange`/`teal` 别名）；DIFF-489 JSON 检查器 Schema 推断/路径复制/路径快捷选择与弹层 `jsonPath` 同步；DIFF-488 JSON 检查器结构面板（键统计/重复键/UTF-8，对齐 Tauri `json-analysis`）；DIFF-487 JSON `validate` 结构摘要（节点数/最大深度，对照 next-tauri `analyzeJson`）；DIFF-486 设置加载/Electron 迁入外观与导航布局规范化（`interfaceStyle`/`customGroups`/`navigationStyle`/`paneSizes` 键，对齐 Electron `settings.test.ts` `normalizes unknown interface styles` / `normalizes custom navigation groups`）；DIFF-485 设置加载/保存/Electron 迁入数值边界 `clampNumber`（对齐 Electron `settings.test.ts` `normalizes numeric settings at their supported boundaries`）；DIFF-484 JSON 检查器缩进写盘/加载回写与 `persistJson` 内存校正（补充 DIFF-483，对齐 Electron `JsonInspector` 仅 2/4）；DIFF-483 JSON 检查器 `formatOptions.spaces` 会话恢复规范化（仅 2/4，对齐 Electron `JsonInspector` 缩进下拉）；DIFF-482 设置/Electron 迁入规范化编辑器字体名（对齐 Electron `settings.test.ts` `normalizes editor font family names`）；DIFF-480 设置加载规范化非法 Vault 树展开模式（对齐 Electron `settings.test.ts` `normalizes unknown vault tree expand modes`）；DIFF-479 Vault Git 分叉后 `fetch` 同时更新 ahead/behind（对齐 `VaultGitService.status`）；DIFF-478 Vault Git `fetch --prune` 清理已删远程 tracking ref（对齐 `vaultGitService.ts`）；DIFF-477 查找替换正则 `$1`/`\\n` 逐匹配展开（对齐 `findReplace.test.ts`）；DIFF-476 Vault 树 collapseAll 保留选中祖先（对齐 `vaultTreeExpand.test.ts`）；DIFF-475 Vault Git 空 URL 删 remote 单测；DIFF-474 Vault Git `fetch --prune`/`behind` 单测；DIFF-473 MCP stdio 协商 + `json_format` 成功/失败往返；DIFF-472 工具注册默认可用 + `testConnection` 11 工具；DIFF-471 MCP `listTools` catalog（描述/schema/readOnlyHint）；DIFF-470 MooTool MCP `strictObject` 入参；DIFF-469 Vault MCP 参数 Zod 边界 + Git push 非快进；DIFF-468 Vault MCP 拒绝相对路径授权；DIFF-467 Vault MCP 授权 `realpath` + 拒绝符号链接根；DIFF-466 `access.json` strictObject + `scripts/README-product-evidence.md`；DIFF-465 Git merge 冲突产品窗准备脚本；DIFF-464 access schema + JSON MCP stdio + IME 准备脚本；DIFF-463 Vault MCP stdio 端到端；DIFF-462 Vault `access.json` 单测 + 冲突产品窗准备脚本；DIFF-461 Vault MCP `vaultAccess.test.ts`；DIFF-460 MCP 工具 `server.test.ts` 边界；DIFF-459 AI `testConnection` MCP 子进程 + JSON `spaces:0`；DIFF-458 JSONPath 弹层双击选路径；DIFF-457 JSONPath 弹层 `JsonPathPickerDialog` +「使用」单测 + 帧 `148`；DIFF-456 JSON 检查器帧 `147`；DIFF-455 JSON 内联路径树双击查询；DIFF-454 JSON 路径树 80 截断 + AI 并发安装；DIFF-453 AI 预览隐私/安装备份；DIFF-452 AI NeedsRepair/卸载保留 Skill；DIFF-451 Codex TOML MCP/Skill 安装单测；DIFF-450 AI 安装写盘失败回滚；DIFF-449 AI 安装 stale/连接失败/符号链接/Skill 单测；DIFF-448 冲突对话框 reload/另存副本点击 + `GitPullGuardTest`；DIFF-447 Git pull merge 守卫顺序 + 冲突对话框点击；DIFF-446 Git 提交守卫与模态禁焦 + reload 单测；DIFF-445 Git init 全流程单测 + 冲突叠层焦点策略；DIFF-444 Vault 冲突 `VaultConflictActions` + Git `.DS_Store`/持锁单测；DIFF-443 Git `index.lock` 恢复与并发提交对齐 Electron；DIFF-442 Git `fileDiffs` 路径规则与 `vaultGitService` 单测补齐；DIFF-441 冲突 reload/keep 会话流 + 删除态 UI 帧 `146`；DIFF-440 JSON 冲突另存副本会话流 + 外部删除 `deleted` 冲突单测；DIFF-439 监视器→冲突（JSON/随手记）+ Git theirs `144` + Vault 冲突 keep `145` + 产品窗冲突手工说明；DIFF-438 JSON/随手记冲突会话 + `143`；DIFF-437 Git 冲突 ours 钮 `142` + 随手记冲突副本 + `vaultGitService` fixture；DIFF-436 Vault 外部冲突保存副本 + 对话框 Tab 帧 `141`；DIFF-435 AI MCP 用户改配置 `Conflict` 单测（对齐 Electron `aiIntegrationService.test.ts`）；DIFF-434 命令盘结果行 Tab 焦点环 + `140`；DIFF-433 侧栏 `NavItem`/分离钮 + 强调色 `AccentSwatches` + `138`/`139`；DIFF-432 侧栏语言 `LanguageRow` Tab 聚焦 + `136`/`137`；DIFF-431 设置导航/语言 `134`/`135`；DIFF-430 JSONPath `pathPickerPreview`；DIFF-429 Electron `jsonTools` fixture + Host 搜索 `133`；DIFF-428 HTTP 集合搜索 `132`；DIFF-427 JSON Vault 搜索 `131`；DIFF-426 复制/单文件导入/冲突副本 `rebaseline`；DIFF-425 Vault 结构性 CRUD `rebaseline`；DIFF-424 Vault 移动/重命名/保存 `noteOwnWrite` 辅助函数；DIFF-423 Vault 复制 `noteOwnWrite`；DIFF-422 Vault 导入 `noteOwnWrite` 抑制监视器误报；DIFF-421 Vault 树多文件拖放导入 `recordVaultActivity`；DIFF-420 `RebBaselineVaultMonitorOnSessionReload`（SideEffect）+ 监视器 `stateLock`；DIFF-419 导入/备份后 `VaultRevisionMonitor.rebaseline`；DIFF-418 `reloadToolSessionsFromStore` 统一 `notify*VaultTreeChanged`（跨产品导入/备份恢复）；DIFF-417 备份恢复 Vault 树刷新；DIFF-416 `VaultRevisionMonitor` 外部变更同样 `notify*VaultTreeChanged` 刷新树；DIFF-415 手动 Git/刷新 Vault 经 `notify*VaultTreeChanged` 重载 snapshot（对齐 Electron `*-vault:changed`→`load()`）；DIFF-414 随手记 Vault 导入打开笔记 `recordVaultActivity`；DIFF-413 `autoPullMinutes`/Vault 根变更重置 `VaultGitPullScheduler`；DIFF-412 Vault CRUD/附件/树拖移对齐 Electron `recordActivity`；DIFF-411 设置打开 `autoDownloadUpdates` 时已有 Available 结果立即下载；DIFF-410 后台 `checkForUpdatesAndBroadcast(automatic)` 仅 `Available` 更新关于页 UI；DIFF-409 `UpdateAutoCheckScheduler` 对齐 Electron 2.5s 首次 + 每小时 `autoCheckUpdates`；DIFF-408 自动 pull 后 `jsonVaultAutoPullTick`/`quickNoteVaultAutoPullTick` 驱动 snapshot + `persistFilter`（对齐 Electron `*-vault:changed`）；DIFF-407 Vault 自动 pull 对齐 Electron 干净工作区（`VaultGitAutoPull`）；DIFF-406 `SessionManagerReloadTest` 锁定 `sessionGeneration`/Vault overlay 重载；DIFF-405 Host/文本对比 `TextFieldValue` 随 `sessionGeneration` 同步；DIFF-404 跨产品导入后 `reloadToolSessionsFromStore` + 迁移预览重置保留导入提示；DIFF-403 设置配置根提示/打开 + 迁移预览随备份重载清空；DIFF-402 设置有效数据目录提示 + 备份路径随 `sessionGeneration` 刷新；DIFF-401 备份恢复后图片/收藏/HTTP/Vault 等监听 `sessionGeneration`；DIFF-400 恢复备份原地重载会话 + `sessionGeneration`；DIFF-399 恢复备份后 `reloadAllToolSessionsFromStore`；DIFF-398 备份路径打开区分 SQLite 与 `config/`；DIFF-397 恢复备份后 `rebindDataDirectories` + 撤销 MCP 数据授权；DIFF-396 设置备份路径「在文件夹中打开」（`openBackupLocation`）；DIFF-395 设置备份路径预览（`BackupInfo` + `data.directory`/Vault 刷新）；DIFF-394 `data.directory` 变更后 Host/HTTP/图片/环境变量/JSON 监视器重载；DIFF-393 `data.directory` 运行时有效 `dataRoot` + SQLite `rebind`；DIFF-392 Vault 根切换关闭 Git/冲突 overlay；DIFF-391 Vault 有效根变更撤销 AI、重置树 UI；DIFF-390 JSON/随手记 Vault 外部变更（删除/路径不在列表仍重载）单测；DIFF-389 `jsonVaultApplyExternalChange`/`quickNoteApplyExternalChange` 干净时总重载当前文件 + 单测；DIFF-388 Vault 刷新立即刷新 Git 角标；DIFF-387 JSON/随手记 Git `onVaultRefresh` 先 `persistFilter`；DIFF-386 Vault 树键盘 Enter 打开文件先 `onSelect`；DIFF-385 随手记 metadata `Untitled` + 单测；DIFF-384 JSON `persistFilter` 重载；DIFF-383 随手记刷新重载 + 单测；DIFF-382 JSON Vault 刷新 `jsonVaultReloadOpenFileIfClean`（对齐 Electron `reloadSelectedFromDisk`）+ 单测；DIFF-381 Vault 树键盘 Enter 选中目录（`selectPath`）+ 单测；DIFF-380 JSON Vault 树目录单击 `prepareJsonVaultContext` + 冲突回滚单测；DIFF-379 JSON Vault 树 `openJsonVaultTreeFile` flush 失败回滚选中 + 单测；DIFF-378 JSON/随手记 prepare 失败回滚优先 `currentFile` + 随手记 Vault 树统一 `prepareQuickNoteVaultContext` + 单测；DIFF-377 随手记 `prepareQuickNoteVaultContext` 保存失败回滚 `vaultSelectedPath`（目录/切换笔记）+ 单测；DIFF-376 JSON 切换片段 prepare flush 失败回滚选中 + 单测；DIFF-375 JSON 目录 prepare flush 失败回滚 `vaultSelectedPath` + 单测；DIFF-374 JSON 目录 `prepareJsonVaultContext`/树单击 flush + 单测；DIFF-373 随手记 idle `quickNoteIdleAutosaveAttempt` + 失败 bump 单测；DIFF-372 随手记无打开笔记 `quickNoteSaveFromUserAction`/`openQuickNoteNewNoteDialog` + 单测；DIFF-371 JSON 无选中保存 `jsonVaultSaveFromUserAction`/`openJsonVaultNewFileDialog` + 单测；DIFF-370 JSON 空闲自动保存 `jsonVaultIdleAutosaveAttempt` + 单测；DIFF-369 JSON 新建文件夹 `jsonVaultRequireFlushDirty` + 复制片段 `jsonVaultFlushDirtyOrNotice` 单测；DIFF-368 `jsonVaultFlushDirtyOrNotice` + 单测；DIFF-367 JSON `jsonGitFlushBeforeAction` + 单测；DIFF-366 随手记 Git `onFlush` 跳过干净文档（含 metadata）；DIFF-365 随手记 `QuickNoteVaultContext` + prepare 冲突单测；DIFF-364 `prepareJsonVaultContext` 提取 + 冲突失败单测；DIFF-363 随手记 `saveIfNeeded`/JSON `prepareJsonVaultContext` 保存失败统一状态栏；DIFF-362 随手记 Vault 打开/拖移保存失败写 error；DIFF-361 Vault 新建默认父路径复用 import 目标目录辅助；DIFF-360 随手记树单击目录先 saveIfNeeded 再选中/清空；DIFF-359 JSON Vault 树打开前 flush；DIFF-358 JSON 结果/转换弹层写入编辑器前 flush；DIFF-357 JSON/随手记历史恢复前 flush/save 脏 Vault；DIFF-356 JSON 工具栏导入前 flush 脏 Vault；DIFF-355 JSON 拖入非 JSON 前 flush + 随手记树拖放 `quickNoteVaultImportTargetDirectory`；DIFF-354 JSON 编辑器拖入 `.json` 按树选中目录入库 + `JsonVaultImport`；DIFF-353 JSON/随手记 Vault 移动默认父路径；DIFF-352 JSON Vault 树多文件拖放导入前 flush；DIFF-351 随手记 Vault 树多文件拖放导入前 saveIfNeeded；DIFF-350 随手记工具栏 Vault 导入选中目录 + 相对路径；DIFF-349 随手记导出纯正文 + 标题.txt 默认名；DIFF-348 随手记文档信息 prepare 上下文；DIFF-347 随手记新建文件夹 saveIfNeeded 守卫；DIFF-346 JSON Vault 底栏复制 prepare；DIFF-345 随手记复制前 prepare 上下文；DIFF-344 JSON Vault 更多菜单树选中 + 复制 prepare；DIFF-343 随手记「更多」菜单 Vault 导入前 saveIfNeeded；DIFF-342 随手记 Vault 导入/拖放打开前 saveIfNeeded；DIFF-341 JSON 拖入 Vault/编辑器打开前 flush 脏片段；DIFF-340 随手记 Vault 导出前 prepare 上下文；DIFF-339 Vault 导出当前打开文件用编辑器缓冲；DIFF-338 Git 角标刷新仅 `runAction` 不挂钩面板 `load`；DIFF-337 Git 面板操作后刷新 Vault 角标；DIFF-336 随手记用户编辑清空状态栏 error；DIFF-335 随手记 `saveIfNeeded` 干净文档跳过保存、不因旧 error 阻塞；DIFF-334 Vault 重命名前 flush/save（复用 `moveAffectsOpenPath`）；DIFF-333 Vault 移动前 flush/save 当前打开文件（`moveAffectsOpenPath`）；DIFF-332 JSON Git flush 跳过已保存文件；DIFF-331 内联路径树预览仅选中行求值；DIFF-330 内联路径树行预览 `inlinePreviewText`；DIFF-329 JSONPath 弹层/内联树预览 `jsonPathNodePreview`；DIFF-328 configure-remote 后 remote 覆盖；DIFF-327 Vault Git remote 字段与 load 同步；DIFF-326 Vault Git 失败非 pull 不刷新状态；DIFF-325 Vault Git 顶栏 sync 不依赖 remote；DIFF-324 Vault Git `runAction` 异常 toast；DIFF-323 Vault Git 面板 load 失败 toast；DIFF-322 Vault Git 加载 Diff 清空/失败 toast；DIFF-321 Vault Git 对话框仅 toast、无底部状态行；DIFF-320 内联路径树 `applyInlinePathTreePreview` + F04 验收链 DIFF-319/313～318；DIFF-319 JSONPath 弹层使用/双击仅选路径（对齐 Electron）；DIFF-318 Vault Git 成功仅 `git.done` toast；DIFF-317 Vault Git 顶栏 noRepo 与空态 unavailable 分工；DIFF-316 Vault Git 不可用态隐藏远程行、继续按钮显隐；DIFF-315 Vault Git 面板布局（提交行/去掉面板内 authHint·counts）；DIFF-314 Vault Git 对话框标题/入口/Diff 区标题；DIFF-313 Vault Git 面板文案对齐 `json.git.*`；DIFF-312 Vault Git「继续/提交」按钮 i18n；DIFF-311 Vault Git 默认提交说明 i18n；DIFF-310 HTTP 打开查找不自动选中首条（`FIND_INDEX_UNSET`）；DIFF-309 HTTP 响应查找按 RSTA 选区 `findNext` 跳转；DIFF-308 查找无匹配仅 info toast（对齐 Electron，不写 notice）；DIFF-307 查找无匹配 `find.noMatches` 文案键；DIFF-306 Host 单次替换清空 notice；DIFF-305 随手记查找 notice + 图片/PDF 历史；DIFF-301～304 notice 对齐切片；DIFF-304 多工具清空不写 cleared；DIFF-303 全工具历史恢复不写 `json.notice.restored`；DIFF-299～302 JSON/Host/随手记 notice 对齐；DIFF-301 Host/随手记全部替换成功不写 `json.find.matches`；DIFF-300 JSON 历史恢复不写 `notice`、查找替换成功 `copyState` idle；DIFF-299 JSON 工具栏清空仅清正文（不写 `json.notice.cleared`）；DIFF-298 JSON `showResult` 成功无 toast（对齐 Electron `showOutput`）+ 检查器顶栏 `MooIconButton` 关闭；DIFF-297 JSON 检查器侧栏标题 `json.action.more`；DIFF-296 JSON 结果弹层复制同步工具栏 `copyState`；DIFF-295 JSON 结果弹层复制同步状态栏 notice；DIFF-294 JSON 格式化/压缩/转义 `transform` 历史摘要对齐 `runTransform`；DIFF-293 JSON 检查器转义 notice + 高级格式化历史 `json.format.apply`；DIFF-292 HTTP 响应 `findIndex` 在打开/新响应/切 Tab/关闭时归零；DIFF-291 JSON 工具栏格式化固定 2 空格 + 工具栏折叠查找保留 `findReplacedCount`；DIFF-290 HTTP 响应查找无匹配 `notice`；DIFF-289 查找条 `find.find` 按钮 + JSON 替换清空 notice；DIFF-288 Host `findReplacedCount` + 全工具查找条 `find.foundPrefix`；DIFF-287 JSON/随手记查找条「已替换」计数（`findReplacedCount` + `find.replacedPrefix`）；DIFF-286 HTTP 响应 `responseEditor` 切页 `ClearOnDispose`；DIFF-285 JSONPath 弹层双击执行 `queryPath`（对齐内联路径树）；DIFF-284 切页 `EditorFindHighlight.ClearOnDispose`；DIFF-283 JSON 查找上/下条与全部替换无匹配状态栏提示；DIFF-282 JSON/随手记 RSTA 查找命中高亮（`EditorFindHighlight`）；DIFF-281 随手记完整 FindReplaceBar + `RstaFindNavigation` / 选区优先 `replaceCurrent`；DIFF-280 查找条键盘（↑/↓/Enter/Esc + 打开聚焦，Host/JSON/HTTP + 随手记 Esc）；DIFF-279 Host 正文查找命中高亮（`FindHighlightTransformation`）；DIFF-278 Host 单次替换后选中下一处匹配；DIFF-277 Host 查找条上/下条与 matchCase/wholeWord/regex；DIFF-276 Host 打开查找预填正文选区；DIFF-275 代码运行编辑器 `Cmd/Ctrl+Enter` 运行；DIFF-274 HTTP 超时 `http.timeoutHint` tooltip；DIFF-273 代码运行编辑器 `Cmd/Ctrl+Shift+F` 格式化；DIFF-272 HTTP 超时写入全局设置 + URL/超时 Enter 发送；DIFF-271 HTTP 请求子面板（Tabs/键值表）聚焦时壳层不打开响应查找；DIFF-270 HTTP 请求 Body 聚焦时壳层不打开响应查找；DIFF-269 HTTP 响应查找预填选区；DIFF-268 RSTA `Cmd/Ctrl+R` 绑定查找；DIFF-267 JSON/随手记/Host `Cmd/Ctrl+R` 打开查找；DIFF-266 JSON/随手记打开查找预填编辑器选区；DIFF-265 JSON XML/Bean 转换成功写入通用历史；DIFF-264 JSON 转换/结果对话框 `json.dialog.*` 与 Electron 文案；DIFF-263 JSON 检查器 XML/Bean 转换 `conversionMode`/`conversionInput` 与 Vault `dialogInput` 分离；DIFF-262 Electron 设置迁入 `schemaVersion < 11` 时 `legacyMigrationHintDismissed = true`（对齐 `mergeSettings`）；DIFF-261 JSON/随手记切页保留 Git·历史·Vault·JSONPath 等模态会话（对齐 Electron 模块级 state）；DIFF-260 迁移提示叠层守卫 + 导入成功自动 dismiss；DIFF-259 Java 迁移提示 `LegacyMigrationHintDialog` + `legacyMigrationHintDismissed`；DIFF-258 `openSettings(categoryId)` + F05 运行时缺失横幅；DIFF-257 设置页 `settingsNavCategoryId` 会话记忆；DIFF-256 Host `profileContextMenuId` 会话态 + Vault/Host 右键菜单首项焦点；DIFF-255 Vault 树 `BringIntoView` 选中滚入视口 + 右键菜单时禁用窗口激活抢焦点；DIFF-254 窗口激活聚焦编辑器（`LocalAwtWindow` + `FocusEditorOnWindowActivate`）；DIFF-253 随手记 `vaultTreeOpen` 宽布局折叠文档库；DIFF-252 JSONPath 选择器 `pathPickerSelection` 会话态；DIFF-251 Vault 树滚动 `vaultTreeScrollOffset` 会话态；DIFF-250 Vault 树展开 `vaultTreeExpanded` 会话态；DIFF-249 Vault 树右键菜单 `vaultContextMenuPath` 会话态；DIFF-248 JSON/随手记 Vault 外部冲突 `vaultConflict` 会话态；DIFF-247 随手记文档信息 / JSON Vault 删除确认 overlay 会话态；DIFF-246 JSON/随手记 Vault Git 面板 `gitDialogOpen` 会话态（分离窗保持打开）；DIFF-245 `OnToolLeaveUnlessDetached` 复用分离守卫；DIFF-244 全工具 `DismissModalOverlaysOnDispose` 统一 `!isDetached`；DIFF-243 图片/调色板分离守卫与 PDF 切页取消；DIFF-242 代码运行/正则/系统信息切页取消在途任务；DIFF-241 HTTP/翻译主窗切页取消在途请求；DIFF-240 JSON 检查器手改 JSONPath 清空旧 pathResult；DIFF-239 PDF/图片/留言板编辑清空状态栏 notice；DIFF-238 多工具用户编辑清空状态栏 notice（含加解密/Protobuf/HTTP/时间/调色板）；DIFF-237 图片切页取消压缩/水印/SVG 协程；DIFF-236 图片工具切页取消区域截图 overlay/`busy`；DIFF-235 调色板切页取消屏幕取色 overlay/`picking`；DIFF-234 全工具切页关闭模态遮罩（`DismissModalOverlaysOnDispose` + `ToolModalOverlaysTest` Host/Image）；DIFF-233 HTTP 切页关闭模态遮罩；DIFF-232 切页关闭 JSON/随手记模态遮罩；DIFF-231 JSONPath Enter 查询与状态栏 notice 显隐；DIFF-230 随手记用户编辑清空 notice；DIFF-229 JSON 内联路径树行组件与截断提示；DIFF-228 JSONPath 选择器对齐 Electron；DIFF-227 JSON 编辑清空检查器 notice/pathResult；DIFF-226 JSON 检查器 `notice`/`pathResult` 优先级；DIFF-225 JSON 检查器结果区优先 `pathResult`；DIFF-224 JSON 新建 Vault 文件前 flush；DIFF-223 随手记新建笔记 `saveIfNeeded` 守卫；DIFF-222 Vault 底栏操作绑定树选中路径；DIFF-221 Vault 删除后 `savedText`/随手记编辑器状态；DIFF-220 JSON 更多菜单前置保存 + 随手记建夹 `resolveEntryPath`；DIFF-219 Vault 对话框单段路径 + 随手记复制前保存；DIFF-218 JSON Vault 复制后选中路径 + 复制前脏保存；DIFF-217 Vault 删除后清除 `vaultSelectedPath`；DIFF-216 Vault 重命名/移动后路径同步；DIFF-215 JSON Vault 树右键前置保存与重命名预填；DIFF-214 Vault 移动目标过滤 + 随手记树右键前置保存 + JSON Vault 对话框文案；DIFF-213 随手记 Action 对话框按钮与重命名预填；DIFF-212 随手记移动文件夹下拉；DIFF-211 随手记删除 Action 对话框；DIFF-210 JSON Vault「新建」路径对话框；DIFF-209 JSON/随手记 Vault 删除确认；DIFF-208 随手记 Vault 右键文档信息；DIFF-207 Vault 树拖入按选中目录 + 随手记 Vault 底栏；DIFF-206 树选中展开不重置手动展开；DIFF-205 Vault 树按选中路径展开祖先（JSON/随手记建夹可见）；DIFF-204 随手记新建空笔记 + JSON 建夹选中路径 + 设置代理字段禁用态；DIFF-203 随手记新建笔记/文件夹落在当前选中目录；DIFF-202 JSON Vault 新建文件/文件夹落在当前选中目录；DIFF-201 Vault Git 调度识别随手记 metadata 脏 + 编辑器手工验收说明 `docs/evidence/2026-09-16-editor-manual-acceptance/`；DIFF-200 随手记文档库树选中/目录清空/状态栏路径；DIFF-199 随手记 250ms 空闲自动保存；DIFF-198 Git 面板 load/刷新不隐式 flush；DIFF-197 JSON Vault 250ms 空闲自动保存 + 树选中/脏标记；DIFF-196 JSON Vault 底栏选中路径/未保存 `•`；DIFF-195 JSON 检查器 `className`/`pathResult` 会话持久化 + JSON/随手记导入导出目录对齐 `tools.exportDirectory`；DIFF-194 JSONPath 查询写入检查器结果区；DIFF-193 相对 Vault 迁后写入本产品根；DIFF-192 Java 自动下载更新；DIFF-191 Java 绝对 Vault 根；DIFF-190 Java 侧栏样式/强调色；DIFF-189 二维码保存目录迁入；DIFF-188 Java HTTP 超时/Host 导出目录；DIFF-187 Java 调色板 `func.colorBoard` 会话；DIFF-186 Java Vault 排序/树展开迁入；DIFF-185 Java 加解密 `digestFilePath`/随机长度会话；DIFF-184 Vault Git 提交/推送前 flush；DIFF-183 Java 二维码图片路径会话；DIFF-182 Java Host 当前方案名选中；DIFF-181 Java 配置 regex/计算器会话 + 时间 Console 解析；DIFF-180 Java ConsoleUtil 计算器/二维码草稿；DIFF-179 Java 时间转换日志草稿迁入历史；DIFF-178 设置保存规范化 + 侧栏「全部」分隔线；DIFF-177 设置 `hiddenNavigationToolIds` 加载规范化；DIFF-176 导航显隐单测 + QrCode 遗留草稿单测；DIFF-175 设置导航工具分组显隐；DIFF-174 设置导航全部显隐 + Electron 明文 proxy/git 迁入；DIFF-173 JSON 检查器 JSONPath→结果→路径浏览分区；DIFF-172 代码运行左右分栏 + `runtime-editor-output` 宽度迁入；DIFF-171 二维码/随手记分栏 + `qrcode-*`/`quick-note-*` 迁入；DIFF-170 Protobuf/加解密分栏 + `protobuf-*`/`crypto-*` 迁入与 `IoPaneLayouts`；DIFF-169 调色板/翻译编辑分栏 + `color-board`/`translation-editor` 迁入；DIFF-168 设置/配置分栏 + `settings-page`/`config-*` 迁入；DIFF-167 编码三列分栏 + `encode-panes` 迁入；DIFF-166 计算器/Cron 分栏 + `calculator-panels`/`cron-builder` 迁入；DIFF-165 正则/格式化文件分栏 + `regex-test`/`reformat-file` 迁入；DIFF-164 文本对比/网络分栏持久化 + `text-diff`/`network-workspace` 迁入；DIFF-163 Electron P5 分栏比例迁入；DIFF-162 JSON 分栏；DIFF-161 迁移 `apply` 识别 `legacyElectronFingerprint`；DIFF-160 Electron `secrets` 密文预览警告；DIFF-159 JSON 检查器 1321dp；DIFF-158 `sourceFingerprint`；DIFF-157 `t_next_migration_run` 整包跳过；DIFF-156 源 SQLite `t_next_migration_row` 过滤；DIFF-155 `import_migration_row` 逐行登记；DIFF-154 迁移 `importDedupeKey` 历史/草稿幂等；DIFF-153 Java FuncConsts 草稿字段对齐；DIFF-152 SQLite `t_func_content` → 工具会话草稿；DIFF-151 Electron `runtime.drafts`/`options` → 代码运行会话；DIFF-150 Electron `mootool-next.json` 设置合并；DIFF-149 Java `config.setting` 补丁；DIFF-148 SQLite P5 迁入；DIFF-147 颜色收藏 + 工具历史/草稿/QR；DIFF-146 正则/Cron + F04 3 MiB format 单测；DIFF-145 设置运行环境检测/浏览；DIFF-144 数据迁移扫描/预览/确认导入；DIFF-143 设置目录选择器 + F04 大 JSON/重复键单测；DIFF-142 默认导出目录绝对路径与加载清除；DIFF-141 Vault 相对路径加载清除与 `effectiveCustomRoot` 运行回退；DIFF-140 AI receipt `.json`、`getStatus`/预览 JSON 修复与安装闭环单测；DIFF-139 文档库绝对路径校验（`VaultPathConfig` 误导入已修）；DIFF-138 Git remote URL 校验；DIFF-137 Git pull merge 冲突；DIFF-136 Git merge 冲突解析/中止/继续；DIFF-135 Git rebase 冲突后继续单测；DIFF-134 AI JSON 重复键 + MCP 11 工具校验；DIFF-133 AI receipt/manageServer；DIFF-132 Vault MCP 只读搜索/读取；DIFF-131 设置 AI 接入 + MCP 子进程首版；DIFF-130 随手记 metadata 脏状态与系统信息 toast；DIFF-128 正则/Cron/UA/网络/代码运行 toast；DIFF-127 编码/配置/加解密运算 toast；DIFF-126 计算器/Protobuf/收藏/图片删除 toast；DIFF-125 JSON XML/Bean 与环境变量删/导出 toast；DIFF-124 JSON 转换 toast + 工具激活 Vault 重载；DIFF-123 QR/PDF/翻译/时间/HTTP/加密/图片 toast；DIFF-122 Vault CRUD/Host/HTTP/格式化 toast；DIFF-121 JSON/随手记/配置导出与随手记导入 toast；DIFF-120 Vault Git toast / 文本对比导入 toast；DIFF-119 格式化/配置导入 toast；DIFF-117～118 JSON/随手记 Vault 树拖放与导入 toast；DIFF-109～116 Vault Git 丢弃/面板 P5/角标/JSON Vault 菜单与移动/编辑器拖放；DIFF-108 中止合并；DIFF-105～106 工具区拖放；DIFF-088～104 P5
- 仍未完成：见下文；目标未达成，不能标产品完成

## 本轮已补上（相对 DIFF-064）

- Vault/检查器/集合/图片库/对比/编码/格式化/配置/正则/Protobuf 等 `mooToolShell`；JSON/随手记/HTTP/运行台/Host `mooEditorFrame`
- 剩余弹层改 `mooDialogSurface`
- 列表行/芯片 `mooFocusClickable`；首页站点链接焦点帧 `126-home-tab-focus.png`
- 命令盘关闭/结果、历史、字体列表、自定义分组 `mooFocusClickable`；选中侧栏/设置导航 `textStrong` + SemiBold
- 控件高/圆角按风格对齐 Electron `--desktop-control-*`；主按钮 prominent；禁用 0.42；工具标题 SemiBold；JSON/HTTP 工作区压平
- hero/claude/miui 选中导航底与字色、弹层/命令盘半径、设置分组与分段按风格块对齐
- 开关无边/inset/矩形、tooltip token、smartisan/miui/claude 遮罩、状态胶囊与 JSON/随手记 10sp 状态栏
- 输入框底改为 workspace/`--surface`，悬停边 `borderControlHover`，smartisan/miui inset 低光
- modern/quiet 未悬停工具按钮边为透明（对照 `.toolbar-button { border: 1px solid transparent }`）
- Material 菜单 surface 接到 workspace；smartisan/miui 按钮悬停保持 raised；命令盘搜索条 toolbar 渐变；弹层底 workspace
- 导航选中/悬停边按风格（modern 无边、smartisan/miui/claude 有边）；选中图标走红/橙/accent；查找条底边
- `shellRadius`/`shellElevation`/`cardRadius` 对齐 editor-shell/inspector-card；工具 Tab 改下划线选中；设置分组圆角与标题字重按风格，见 [DIFF-075](../../diff/075-style-shell-tabs-header.md)
- 复制/保存/备份走应用内 toast（最多 4 条、3200ms、可关闭）；HTTP 请求/响应 Tab 下划线；侧栏 icon-ghost 30dp，见 [DIFF-076](../../diff/076-style-toast-http-tabs-ghost.md)
- 文本复制统一 `AppContainer.copyText`；图片/二维码复制同样 toast；首页分区与计算器面板走 `cardRadius`，见 [DIFF-077](../../diff/077-copy-toast-card-radius.md)
- Vault 树行 36dp/`--control` 悬停选中；分栏手柄 10dp 强调色中线；检查器卡片底 `--surface`，见 [DIFF-078](../../diff/078-style-vault-node-pane-resizer.md)
- 首页改为分区线/贡献者字头/作品行并修正 Gitee 外链；常用正则两列卡片；计算器 log 与运行台/时区状态胶囊，见 [DIFF-079](../../diff/079-style-home-patterns-status-pill.md)
- 历史行/强调色环/HTTP·Host 集合/快速替换紧凑按钮/快捷键 kbd/YAML 校验底，见 [DIFF-080](../../diff/080-style-history-swatch-collection-kbd.md)
- 上下文菜单 30×11、收藏行、Cron 两列运行、正则命中卡、随手记色点触发器，见 [DIFF-081](../../diff/081-style-menu-favorite-cron-regex.md)
- 紧凑搜索、HTTP 参数表、剩余下拉改 MooMenu、翻译/图片行与 UA 结果格，见 [DIFF-082](../../diff/082-style-compact-search-http-table-menus.md)

## 仍未完成（目标范围内，不能标整产品完成）

### 样式

- Electron 六套 CSS 的逐选择器皮肤（token + hover/shadow/卡片又补了一层，仍不是 CSS 移植）
- 首页以外其余页面的产品窗 Tab 走查仍缺（JSON Vault 搜索、HTTP 集合搜索、JSON「复制」外描边、首页站点链接已有帧）

### 功能

- 产品主窗已有 JSON **Alt 拖选**（`115`）与 **列选写入 ASCII `x`**（`122`）；随手记闩锁列写入见 `125-quicknote-column-latch.png`。系统 IME 预编辑仍未手工验收
- 托盘取色/截图权限对话框手工验收
- P7 三平台安装/升级/卸载/公证

### 验收缺口

- 多数工具已有运行窗口截图，仍保持「待验收」
