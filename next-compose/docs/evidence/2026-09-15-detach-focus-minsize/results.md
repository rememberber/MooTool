# 证据：分离窗口聚焦与最小尺寸提示（2026-09-15）

- 命令：`JAVA_HOME="$(/usr/libexec/java_home -v 21)" ./gradlew :composeApp:desktopTest --offline`
- 结果：`desktopTest` **217/217**（0 skipped / 0 failed）
- 本机：macOS Darwin 25.6.0 x86_64，Zulu 21

## 覆盖

- 已分离工具打开时请求聚焦既有窗口；首页不可分离
- 侧栏窗口按钮在折叠或隐藏标题时关闭，右键仍可用
- 最近列表排除首页、去重、最多 5 条
- 中/英/日分离占位文案替换 `{tool}`
- 宽或高低于 960×640 显示最小尺寸提示

## 未测

- 真实窗口内点击侧栏 ⧉、命令搜索前置拆出窗
- 窗口管理器强制小于 960 的真实截图
- IME/列编辑手工、托盘权限对话框、三平台安装
